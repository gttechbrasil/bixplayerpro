# M5 — Issues

Registro único de correções do marco 5. Nada aqui é funcionalidade nova: são defeitos,
endurecimento ou resíduos encontrados na revisão, na homologação e nos testes em hardware.

Gravidade: **alta** (segurança, perda de dados, app inutilizável), **média** (funcionalidade do
Anexo I com desvio), **baixa** (cosmético, melhoria de robustez). Status: `aberta`, `corrigida`
(commit indicado), `aceita` (risco documentado, sem correção nesta entrega).

| Id | Origem | Gravidade | Descrição | Status |
|---|---|---|---|---|
| M5-001 | Revisão de segurança | baixa | `GET /api/openapi.json` respondia em produção (a UI `/api/docs` já estava desativada), expondo o esquema completo da API. | corrigida — `openapi_url=None` quando `APP_ENV=production` |
| M5-002 | Revisão de segurança | média | `POST /device/register` (público, cria linha no banco) e `GET /device/config` não tinham limite de requisições; um cliente podia inflar a tabela `devices` ou martelar o banco. | corrigida — `app/core/ratelimit.py`: por IP e por dispositivo, com `Retry-After`; variáveis `DEVICE_*` no `.env` |
| M5-003 | Revisão de segurança | baixa | `PUT /reseller/branding` e `PATCH /admin/resellers/{id}` aceitavam `logo_url`/`bg_url` com qualquer esquema (ex.: `javascript:`), embora banners já fossem validados. | corrigida — só `http(s)://`, com teste |
| M5-004 | Revisão de segurança | baixa | Trocar/redefinir a senha de uma revenda não invalida sessões já abertas (JWT sem revogação; expira em até 12 h). Bloquear a revenda **é** imediato, pois cada requisição confere `is_blocked`. | aceita — mitigação: bloquear a revenda quando houver suspeita de sessão roubada; revogação por versão de senha fica para a fase 2 |
| M5-005 | Revisão de segurança | baixa | Excluir uma revenda no admin deixa os dispositivos dela desvinculados (`reseller_id = NULL`) em vez de apagá-los; eles podem ser reivindicados por outra revenda. | aceita — comportamento intencional (o MAC pertence ao aparelho, não à revenda); documentado no manual do admin |
| M5-006 | Endurecimento | média | Sem HSTS, sem `Permissions-Policy` e sem CSP no painel; sem limite de tamanho de corpo no proxy. | corrigida — `deploy/Caddyfile` |
| M5-007 | Teste de carga | — | Primeira rodada de 2.000 dispositivos em 60 s mostrou p95 = 618 ms e p99 = 8 s; repetida com a máquina ociosa deu p95 = 16 ms e p99 = 17 ms (os picos vieram de outro processo compilando na máquina de teste, não da API). Ver `SECURITY-REVIEW.md` §4. | fechada — sem gargalo na API |
| M5-008 | Limpeza (bloco 0) | baixa | Três dispositivos avulsos de teste no banco de produção (`02:50:50:2B:BC:8A`, `02:50:50:07:22:08` e `02:50:50:8C:A5:7B`, este último criado em 07/09/2026 ao validar a 1.2.0 no AVD contra produção; todos sem revenda e sem playlist). | aberta — o comando de exclusão pelo `psql` precisa ser rodado por você (a sessão do Claude Code não tem permissão para escrever no banco de produção); ver `PLANO-M5.md` bloco 0 |
| M5-009 | Limpeza (bloco 0) | baixa | Resíduos de demonstração em produção: revenda `revenda01`, dispositivo `AA:BB:CC:00:11:22` ("Cliente Demo") com playlist `http://servidor.exemplo`, 2 Pix pendentes do sandbox (R$ 35,00, 05/09/2026). | aberta — remover junto com a revenda de teste no bloco 3 (com você) |
| M5-010 | Validação M4 (emulador) | baixa | A tecla **GUIDE** do controle não chega ao app no emulador (`KEYCODE_GUIDE` é interceptado pelo sistema); o guia abre pelo chip "Guia". Confirmar em TV box real com controle físico. | aberta — depende do bloco 1 |
| M5-011 | Validação M4 (emulador) | baixa | PiP, recorte da câmera (notch) e o giro de 180° do player no celular só foram testados no emulador; decodificação libVLC só em x86_64. | aberta — depende do bloco 1 |
| M5-012 | Marca (bloco 3) | média | No celular, o primeiro boot após a ativação não sincronizava a playlist (a `PlaylistViewModel` hospedada no nav host lia o config uma única vez, antes de ele chegar); a lista só aparecia ao reabrir o app. | corrigida — a view model segue `repository.state`; validado com instalação limpa no AVD |
| M5-013 | Hardware (cliente) | alta | O app fecha às vezes ao reproduzir filme na TV box; após reiniciar a box o filme roda. Suspeitas: crash nativo do libVLC em ARM, OOM com catálogo grande ou troca de engine/SurfaceView. Sem adb no cliente. | corrigida (instrumentação, 1.2.1) — captura de crash Java (`UncaughtExceptionHandler`), motivo da última morte do processo via `ApplicationExitInfo` (crash nativo, OOM, ANR, com tombstone quando o sistema guarda), log em arquivo, `logcat` do próprio processo; botão **Configurações → Enviar diagnóstico** e envio automático da evidência de crash no boot seguinte para `POST /api/v1/device/diagnostics` (≤ 512 KB, 10 por dispositivo, listado no admin no detalhe do device). Com o hardware confirmado (MXQ Pro 4K 5G, **1 GB de RAM**, catálogo de ~20k filmes) a hipótese principal passa a ser **OOM**; medidas de memória em M5-018, decodificação em M5-020. O diagnóstico agora inclui `sdk_int`, `release_reported` (não confiável), `security_patch`, `hardware`, RAM total/disponível, `low_ram` e a lista de decodificadores de vídeo com HW/SW. **08/09/2026, 1.2.2 na box**: cliente relata funcionamento normal; diagnóstico sem crash Java nem evidência de morte do processo em 3 reproduções (decodificador AVC de HW). Status: **em observação** — o SDK 29 não tem `ApplicationExitInfo`, então uma morte por memória só apareceria como ausência de crash file; se voltar a fechar, o cliente envia novo diagnóstico logo após reabrir |
| M5-014 | Hardware (cliente) | alta | Séries: clicar no episódio só seleciona; 2–3 cliques para reproduzir. Filmes: idem no "Assistir". | consequência do M5-017 (a box abria a UI de celular, que não tinha foco por D-pad). Mesmo assim endurecido na UI de TV: foco inicial com nova tentativa até ser concedido (`requestFocusWithRetry`), clique sempre no KEY UP do OK/ENTER no mesmo nó que tem o foco, sem `clickable` paralelo; teclas OK registradas no diagnóstico |
| M5-015 | Cliente | média | Campo MAC sem máscara no painel. | corrigida — máscara `AA:BB:CC:DD:EE:FF` ao digitar e ao colar (com ou sem separadores), maiúsculas, 17 caracteres; `web/src/lib/mac.ts` |
| M5-016 | Hardware (cliente) | baixa | Registrar em `ANDROID.md` o resultado do teste em hardware quando o cliente informar modelo e versão do Android. | corrigida — MXQ Pro 4K 5G registrada como **dispositivo de referência mínimo** (`ANDROID.md` §13) com os dados reais do diagnóstico de 08/09/2026: **SDK 29 (Android 10)**, string "11.1" falsa, **Allwinner H616** (`sun50iw9p1`), armeabi-v7a, decodificadores de HW para AVC/HEVC/VP9/MPEG-2, `totalMem` mal reportado (M5-021) |
| M5-017 | Hardware (cliente) | crítica | Na TV box (Android AOSP, não Android TV) o D-pad não funciona e o cliente usa o modo mouse: o launcher dispara `LAUNCHER` e abria a `MobileActivity` (UI de toque). | corrigida (1.2.1) — entrada única `LaunchActivity` com os dois intent-filters; UI de TV quando `UiModeManager` = televisão, feature `leanback`/`television` ou **sem touchscreen**; override em Configurações → Modo de interface (Automático/TV/Celular), persistido; UI de celular navegável por D-pad (MENU leva o foco à barra inferior). **Confirmado na box em 08/09/2026**: sinais `uiModeTv=true leanback=true tvFeature=true touchFeature=false touchInput=false` → UI de TV pelo automático (a box tem leanback; o launcher dela é que dispara `LAUNCHER`). Validado antes nos AVDs: TV → UI de TV, celular → UI de celular, ciclo Automático → TV → Celular → Automático pelo controle (a UI de celular rodando no AVD de TV só com D-pad). Nas imagens Android 16 o `hw.touchScreen=no` não remove o toque (dispositivos virtio continuam), mas o AVD `bix_box_lowend` (Android 9, imagem de celular com perfil `tv_1080p`) fica **sem dispositivo de toque** e a entrada única escolheu a UI de TV automaticamente — a simulação da box AOSP funcionou aí. A decisão também está coberta por teste unitário (`UiModeDecisionTest`) e o diagnóstico registra os sinais (`ui_signals`) |
| M5-018 | Hardware (cliente) | alta | Consumo de memória com catálogo grande numa box de 1 GB: risco de o sistema matar o app ao abrir o player (LMK) — a manifestação vista pelo cliente em M5-013. | corrigida (1.2.2) — `DeviceClass.lowRam` (RAM ≤ 1,5 GB ou `isLowRamDevice`): cache de imagens do Coil em 5 % do heap, bitmaps RGB_565, cache em disco de 48 MB; Paging com página 24 e `maxSize` 120 (páginas longe da tela são descartadas); cache de posters esvaziado ao abrir o player e em `onTrimMemory`; `largeHeap=false` explícito; buffers do Media3 limitados (20 s / 12 MB) e vídeo ≤ 1080p. Medido no AVD de 1 GB / Android 9: filme tocando 93–97 MB de PSS, canal HLS 111 MB, TS 119 MB, VLC 118 MB, grade de 20k filmes ≤ 106 MB, sem kills (`ANDROID.md` §13) |
| M5-019 | Processo | média | Não havia ambiente de teste parecido com a box do cliente. | corrigida — AVD `bix_box_lowend` (Android 9 / API 28, 1 GB de RAM, GPU por software) incluído no smoke test de reprodução; receita em `ANDROID.md` §13 |
| M5-020 | Hardware (cliente) | média | Boxes com decodificação HW só de H.264: streams HEVC caem no decodificador de software (lento) ou falham. | corrigida (1.2.2) — sem decodificador HEVC de hardware o `DefaultTrackSelector` prefere a variante H.264 quando a playlist oferece (HLS multi-variante); stream só HEVC gera erro de decodificador → fallback automático para o VLC (regra existente 3000–5999). Regra de código: nenhuma decisão usa `Build.VERSION.RELEASE`, só `SDK_INT` (conferido: a única leitura de `RELEASE` é informativa, no diagnóstico). Na box do cliente o diagnóstico mostrou **HEVC com decodificador de hardware** (`OMX.allwinner.video.decoder.hevc`), então lá a preferência por H.264 não é acionada; a regra continua valendo para boxes que só decodificam H.264 |
| M5-021 | Diagnóstico (cliente) | baixa | Na MXQ Pro 4K 5G `ActivityManager.MemoryInfo.totalMem` devolve 16 MB (firmware), o que deixaria `lowRam` certo por acaso e a RAM real desconhecida no diagnóstico. | corrigida (1.2.3) — `DeviceClass` lê `MemTotal` de `/proc/meminfo` quando o valor do `ActivityManager` é implausível (< 256 MB); diagnóstico grava `meminfo_total_mb`. Efeito colateral corrigido: até a 1.2.2 o `lowRam` ficava `true` por acidente nessa box; com 1 GB reais continua `true`, com ≥ 1,5 GB passa a `false` — a confirmar no primeiro diagnóstico da 1.2.3 |
| M5-022 | Admin (cliente) | baixa | O admin mostrava a versão do app que o dispositivo tinha na ativação (`app_version` só era gravado no `register`): o device do cliente aparecia como 1.2.0 rodando 1.2.2. | corrigida (1.2.3) — o app envia `X-App-Version`/`X-App-Type` em toda chamada e `GET /device/config` atualiza os campos quando mudam |
| M5-023 | Painel (cliente) | **crítica** | Desde a 1.2.1 (M5-015) o painel recusava qualquer MAC em "Adicionar dispositivo" com "É preciso que o formato corresponda ao exigido", em todo navegador. O atributo `pattern="[0-9A-F]{2}(:[0-9A-F]{2}){5}"` era compilado pelo Svelte com `{2}`/`{5}` como expressões, virando `[0-9A-F]2(:[0-9A-F]2)5`; a validação nativa do navegador barrava o envio antes da API. Cadastros do cliente em 07/09 passaram porque foram feitos antes do deploy da 1.2.1. | corrigida — `pattern={"…"}` (string literal); reproduzido e verificado com Playwright em WebKit e Chromium (digitado, colado, minúsculo, sem separador). Regra: em atributos Svelte, regex com quantificador `{n}` sempre como expressão string (`configuracoes` já fazia isso) |
| M5-024 | Painel (cliente) | alta | Quem tinha sessão de admin e de revenda no mesmo navegador não conseguia entrar no painel da revenda: o login respondia 200, mas `GET /auth/me` devolvia o admin (cookies separados, admin com precedência) e o layout do painel redirecionava de volta ao login, sem mensagem. Reproduzido em produção com Playwright (WebKit) a partir do log do Caddy do cliente. | corrigida — `GET /auth/me?as=admin|reseller`: cada painel pede o próprio papel; sem o parâmetro o comportamento antigo é mantido. Contorno até o deploy: sair do admin ou usar janela anônima |
| M5-025 | Hardware (cliente) | alta | Suporte a **Amazon Fire TV Stick** (Fire OS = Android sem Google Play Services). Pedido em 14/09/2026; o id M5-022 sugerido já estava em uso. | validada no AVD AOSP, aguarda hardware real; entra no próximo release junto com as demais correções — ver `ANDROID.md` §14: (1) auditoria de dependências: nenhuma referência a GMS/Firebase/Play Core no código nem na árvore transitiva de release (Coil usa OkHttp, Media3 sem extensão Cast, WorkManager usa JobScheduler); (2) `amazon.hardware.fire_tv` entrou como sinal extra do `UiModeDecider` (teste unitário); (3) teclas do controle Fire TV cobertas — Select/OK, D-pad, BACK, MENU (faixas), Play/Pause, REW/FF; nada depende de dígitos, GUIDE, INFO ou teclas coloridas; (4) validação no AVD Android 9 AOSP sem Google APIs; (5) instalação via Downloader e limitações documentadas |
| M5-026 | Painel (cliente) | **crítica** | Painel inutilizável no celular ("no PC tá top, no celular tá tudo bugado"): a barra lateral de 240 px era fixa em qualquer largura, sobrando ~130 px de conteúdo num aparelho de 390 px, e o `overflow-hidden` da raiz **cortava** o resto em vez de rolar. Tabelas de 6–8 colunas, barra de ferramentas e cabeçalho também não tinham tratamento para telas estreitas. | corrigida — abaixo de `lg` a barra lateral virou **gaveta** com botão de menu no cabeçalho (fecha ao navegar, no Esc e no fundo); a página passa a rolar naturalmente no celular (`min-h-dvh`, cabeçalho fixo no topo); `DataTable` ganhou um snippet `card` e mostra **cartões** em vez de tabela abaixo de `md` (Dispositivos, Revendedores, Pagamentos, Auditoria), com busca/filtros empilhados e "selecionar todos" como botão; tabela de Banners passou a rolar em vez de ser cortada; modais viram folha inferior no celular; alvos de toque em 44 px (campos, botões, menu). Verificado com Playwright em 390 px (0 px de overflow em todas as páginas do painel e do admin), 768 px e 1440 px — o desktop segue idêntico. Capturas em `docs/screens/web/mobile/` |
| M5-027 | Painel e admin (cliente) | alta | Caixas de pesquisa das listas perdiam letras enquanto o usuário digitava e o backspace parecia apagar duas. Cada tecla arma um debounce de 350 ms que navega; ao voltar, o `load` devolvia `search` e o `DataTable` copiava esse valor de volta para o campo, apagando o que foi digitado durante a ida e volta. Só aparece com latência de rede — invisível na API local, evidente em produção. | corrigida — o eco só é aceito quando o campo **não** está em foco (link direto, voltar do navegador, limpar filtro); enquanto o usuário digita, o valor do campo é dele. Teste de regressão em `web/e2e/search_race.py` injeta 500 ms de latência e cobre digitação a 400/250/120 ms por tecla, backspace, link direto e limpeza; sem a correção ele reprova ("josefina" vira "jsfn") |
| M5-028 | App (cliente) | alta | **Favoritos** na tela inicial abria a lista de todos os canais. A entrada de TV ao vivo e a de Favoritos são o mesmo destino com argumentos diferentes; com `launchSingleTop` o Navigation reaproveitava a entrada existente e o ViewModel já tinha lido o escopo antigo. Só acontecia depois de visitar TV ao vivo uma vez — por isso passou nos testes. | corrigida — as entradas de menu substituem o destino (`popUpTo` + `launchSingleTop`) e o `LiveViewModel` ganhou `applyScopeKey`, chamado pela tela quando o argumento muda. Verificado com 2 canais favoritados: Favoritos abre com os 2, não com os 1200 |
| M5-029 | App (cliente) | alta | Os layouts não funcionavam no celular: o app pulava a tela inicial e ia direto para TV ao vivo, então a escolha da revenda não aparecia em aparelho nenhum que não fosse TV. | corrigida — a tela inicial agora existe nas duas famílias, com uma aba **Início** na barra inferior e uma renderização própria de cada layout em retrato (`ui/screens/home/HomeLayoutsCompact.kt`), tudo rolando na vertical e com as fileiras de capas rolando na horizontal |
| M5-030 | App (cliente) | alta | No celular a tela inicial ficou com as opções **duplicadas**: TV ao vivo, Filmes e Séries apareciam no layout e de novo na barra fixa de baixo, na mesma tela. Efeito colateral da M5-029, que trouxe a tela inicial para o telefone sem tirar a barra dela. | corrigida (1.5.1) — a barra inferior não aparece mais na tela inicial: ali o menu é o próprio layout. Dentro de TV ao vivo, Filmes, Séries e Guia a barra continua, com **Início** para voltar. Verificado no AVD de celular: a tela inicial cita cada seção uma única vez e a barra reaparece ao entrar numa delas |
| M5-031 | App (cliente) | **crítica** | Detalhe de filme e de série no celular usavam o desenho da TV: 56 dp de margem mais um cartaz de 260 dp não cabem em 390 dp, então o título aparecia como "Fil", a sinopse quebrava uma letra por linha e o botão **Assistir** virava um quadrado laranja sem texto. Na prática não dava para assistir pelo telefone. | corrigida (1.5.2) — as duas telas ganharam desenho em retrato: cartaz no topo, título, botões de largura total e sinopse abaixo; na série a lista de episódios ocupa o resto da altura. Verificado no AVD de celular |
| M5-032 | App (cliente) | alta | "Deita na tela mas não preenche igual à Netflix": o vídeo girava para paisagem e ficava com tarjas pretas largas dos dois lados. Em 16:9 numa tela de 20:9 ou 22:9, manter a proporção original sobra tela nas laterais. | corrigida (1.5.2) — a preferência de ajuste passou a ser de três estados; sem escolha do usuário, **celular preenche a tela** e **TV mantém a proporção** (que já casa com 16:9). O botão do canto e o painel do MENU continuam alternando, e a escolha manual prevalece |
| M5-033 | Admin (cliente) | alta | "Gerenciar revendedor" no celular abria em modo computador: a página media 756 px num aparelho de 390 px. As tabelas da página ficam dentro de itens de grade, e um item de grade não encolhe abaixo do conteúdo (`min-width: auto`), então a tabela esticava o item e a página inteira em vez de rolar na própria caixa. | corrigida — `min-w-0` nos cartões e nas grades dessa página. Medido com Playwright a 390 px: de 756 px para 390 px, sem transbordo, tabelas rolando dentro dos cartões |

## Como abrir uma issue

1. Reproduza e anote: aparelho/navegador, passos, resultado esperado × obtido, `adb logcat` ou
   resposta da API quando houver.
2. Acrescente uma linha na tabela com o próximo id e a origem (`Homologação`, `Hardware`,
   `Revisão`, `Carga`).
3. Correções entram em commits pequenos (`fix(escopo): …`) que citam o id.
