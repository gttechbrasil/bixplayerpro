<?xml version="1.0" encoding="utf-8"?>
<!--
  IPTV servers supplied by resellers are overwhelmingly plain HTTP, so cleartext has to be
  allowed for arbitrary hosts. Our own API is always reached over TLS in release builds;
  only the emulator loopback (10.0.2.2) is expected to be cleartext during development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">bixplayer.pro</domain>
    </domain-config>
</network-security-config>
